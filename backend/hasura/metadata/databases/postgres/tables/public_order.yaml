table:
  schema: public
  name: order
array_relationships:
  - name: carriers
    using:
      foreign_key_constraint_on:
        column: order_id
        table:
          schema: public
          name: order_carriers
  - name: protocols
    using:
      foreign_key_constraint_on:
        column: order_id
        table:
          schema: public
          name: protocol
