import { Container } from '@mui/material';

const Orders = () => {
  // TODO: subscribe single order via id from route

  return (
    <Container sx={{ height: '100%', pt: 2 }}>
      <span>TODO: order details</span>
      {/* <MaintenanceOrder order={selectedOrder} onNavigateBack={resetSelection} /> */}
    </Container>
  );
};

export default Orders;
