const isBroken = (motionData: {xAcceleration: number, yAcceleration: number, zAcceleration: number}[]) => {

  if(motionData.length < 10) 
    return false;
  
  motionData.forEach((data, index) => {


  });
  
};

export default isBroken